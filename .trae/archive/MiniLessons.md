Это детализированная и амбициозная продуктовая логика. Чтобы реализовать концепт «Дерева технологий Civilization» с тремя ветками (Обучение, Закрепление, Повторение) и методикой *I do / We do / You do*, необходимо углубить существующую архитектуру.

Ниже представлена пошаговая логика реализации **StudyNinja 2.0** на базе вашего стека (Neo4j, FastAPI, React).

### Этап 0: Модель Данных (KnowledgeBaseAI)

Существующий граф `Topic -> Skill` нужно расширить. Теперь каждый **Топик** — это контейнер, содержащий контент для трех веток.

**Обновление схемы Neo4j:**
1.  **Узел `ContentUnit` (Мини-урок):**
    *   Свойства: `type` ('intro', 'generic_theory', 'example_solution', 'problem').
    *   Свойства: `complexity` (float).
2.  **Связи:**
    *   `Topic -[:HAS_LEARNING_PATH]-> List[ContentUnit]` (ветка Обучение)
    *   `Topic -[:HAS_PRACTICE_PATH]-> List[ContentUnit]` (ветка Закрепление)
    *   `Topic -[:HAS_MASTERY_PATH]-> List[ContentUnit]` (ветка Повторение)

---

### Этап 1: Онбординг и Профилирование (StudyNinja-API & Front)

Прежде чем строить карту, нужно откалибровать систему.

#### Шаг 1.1: Психотип и "Личность" ИИ
*   **Действие:** Пользователь проходит тест (например, укороченный MBTI или тест на стиль обучения VARK).
*   **Хранение:** В PostgreSQL (`user_profile`).
    *   Пример: *"Визуал, тревожный, предпочитает короткие сессии"*.
*   **Применение:** Эти данные становятся **System Prompt** для LLM (ИИ-репетитора).
    *   *Prompt:* "Ты репетитор. Ученик тревожный — хвали его чаще. Он визуал — используй эмодзи и проси генерировать диаграммы."

#### Шаг 1.2: Выбор Цели (Scope Графа)
*   **Действие:** Выбор ЕГЭ/ОГЭ/Олимпиада.
*   **Логика:** Система делает выборку подграфа из Neo4j (суб-граф, относящийся только к выбранной цели).

#### Шаг 1.3: Диагностическое тестирование
*   **Логика:** Алгоритм "Бинарный поиск".
    1.  Берем ключевые темы середины графа. Даем тест.
    2.  Решил? Идем по связям вверх (к более сложным).
    3.  Не решил? Идем по связям вниз (к пререквизитам).
*   **Результат:** Формирование начальных весов `User -[PROGRESS {weight: X}]-> Topic`.
    *   `weight > 1.0`: Тема сложная / не пройдена (Красная зона).
    *   `weight < 0.5`: Тема легкая / известна (Зеленая зона).

---

### Этап 2: Визуализация "Дерева Технологий" (StudyNinja-Front)

Здесь реализуется метафора Civilization.

*   **UI Компонент:** Каждый предметный блок (например, "Квадратные уравнения") отображается как гекс или узел.
*   **Внутри узла (при клике или зуме):** Раскрываются 3 параллельные ветки (Progress Bars).
    1.  🔵 **Обучение (Learning):** Доступно, если открыт пререквизит.
    2.  🟠 **Закрепление (Consolidation):** Блокировано, пока "Обучение" < 80%.
    3.  🟢 **Повторение (Repetition):** Блокировано, пока "Закрепление" < 90%.
*   **Зависимость веток:** Фронтенд проверяет статус предыдущей ветки перед разблокировкой кнопки "Start" на следующей.

---

### Этап 3: Логика Уроков (StudyNinja-API + LLM)

Это самое сложное ядро логики с `I do / We do / You do`.

#### Кластер 1: Обучение (Learning Branch)
*   *Цель:* Понять концепцию. Веса **не меняются**.
*   **Сценарий урока:**
    1.  **I Do (Теория):** API отдает контент + LLM (с учетом психотипа) пересказывает теорию. "Смотри, как я это решаю...".
    2.  **We Do (Совместно):** Появляется задача.
        *   Кнопка "Подсказка": ИИ дает наводящий вопрос (не ответ!).
        *   Пользователь вводит часть решения.
    3.  **You Do (Сам):** Пользователь решает простую задачу.
    *   *Финал:* Если `You Do` провален — ИИ предлагает разобрать ошибку, но вес в графе не растет (безопасная среда).

#### Кластер 2: Закрепление (Consolidation Branch)
*   *Цель:* Набить руку. Веса **изменяются**.
*   **Сценарий:**
    1.  **We Do:** Сложный пример с минимумом подсказок.
    2.  **You Do (Серия):** 3-5 задач подряд.
*   **Аналитика решений (Score Calculation):**
    Система считает `LessonScore = (Accuracy * 0.6) + (SpeedFactor * 0.2) + (MethodOptimal * 0.2)`.
    *   *MethodOptimal:* Сверяем шаги решения пользователя с эталонными методами из графа (`Method` node).
*   **Динамика весов:**
    *   Высокий Score -> `dynamic_weight` уменьшается (тема освоена).
    *   Низкий Score -> `dynamic_weight` растет. Система рекомендует вернуться в ветку "Обучение".

#### Кластер 3: Повторение (Repetition / SRS Branch)
*   *Цель:* Не забыть (Spaced Repetition).
*   **Сценарий:** Только **You Do**. Задачи максимальной сложности.
*   **Триггер:** Эта ветка "загорается" (становится активной) через интервалы (1 день, 3 дня, 7 дней) после прохождения Закрепления.
*   **Влияние:** Сильно влияет на глобальный рейтинг готовности к экзамену.

---

### Этап 4: Адаптивная Математика (Neo4j & API Logic)

Как именно пересчитываются связи, чтобы карта менялась?

**Алгоритм пересчета (Python/FastAPI):**

```python
async def recalculate_user_graph(user_id, topic_id, session_results):
    # session_results содержит: time_spent, hints_used, errors_count, correctness
    
    # 1. Базовая формула сложности
    performance = (session_results.correctness * 1.0) - (session_results.hints_used * 0.1)
    
    # 2. Получаем текущий вес связи в Neo4j
    current_weight = await neo4j.get_weight(user_id, topic_id)
    
    # 3. Адаптация
    if session_results.branch == "consolidation":
        if performance > 0.8:
            new_weight = current_weight * 0.9  # Становится легче, путь "короче"
        else:
            new_weight = current_weight * 1.15 # Становится сложнее
            
    # 4. Проверка Пререквизитов (Recursion)
    # Если тема провалена, проверяем веса её "родителей" (пререквизитов)
    if performance < 0.4:
         prerequisites = await neo4j.get_prereqs(topic_id)
         # Рекомендация: "Вернись к теме Х, у тебя там пробел"
         return Recommendation(goto=prerequisites[0])
         
    await neo4j.update_weight(user_id, topic_id, new_weight)
```

---

### Техническая Реализация по Репозиториям

1.  **KnowledgeBaseAI (Constructor):**
    *   В `kb_builder.py` добавить генерацию структуры "3 ветки" для каждой темы.
    *   Написать промпты для LLM, чтобы сгенерировать контент "I/We/You" для тысяч тем автоматически.

2.  **StudyNinja-API (Logic):**
    *   Эндпоинт `/api/lesson/start`: Принимает `topic_id` и `branch_type`. Возвращает структуру урока.
    *   Эндпоинт `/api/lesson/submit_step`: Принимает шаг решения. LLM проверяет логику (не просто ответ), возвращает фидбек.
    *   Background Worker: Пересчитывает веса (чтобы пользователь не ждал ответа API, пока идет тяжелая математика).

3.  **StudyNinja-Front (UX):**
    *   **Graph Engine:** Использовать библиотеку типа `React Flow` с кастомными узлами. Узел — это мини-компонент с тремя прогресс-барами.
    *   **Chat Interface:** В правой части экрана всегда висит аватар ИИ-репетитора (анимированный), который реагирует на действия (радуется, задумывается).

### Резюме пользовательского пути

1.  Вася заходит, выбирает "Сдать ЕГЭ Профиль".
2.  ИИ определяет Васю как "Импульсивного, любит практику".
3.  Карта показывает: "Тригонометрия" закрыта, "Алгебра" открыта.
4.  Вася жмет "Алгебра -> Квадратные уравнения".
5.  Видит 3 ветки. Активна только "Обучение".
6.  Проходит ИИ-урок (I Do), ИИ шутит. Ветка заполняется.
7.  Открывается "Закрепление". Вася решает сам. Ошибается.
8.  Система говорит: "Ты поспешил (учли психотип). Вернись на шаг назад в Обучении". Вес темы вырос.
9.  Вася перепрошел, вес упал. Тема позеленела.
10. Открылся путь дальше к "Неравенствам".
