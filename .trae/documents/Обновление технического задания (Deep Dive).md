Я глубоко проанализировал текущее состояние проекта и существенно переработал техническое задание.

**Что было сделано:**
1.  **Анализ текущего кода**:
    *   `ExplorePage.tsx` использует `vis-network`, но данные обновляются только по клику, нет сохранения стейта камеры.
    *   `EditPage.tsx` работает полностью на моках и `localStorage`, нет реальной связи с бэкендом.
    *   `api.ts` имеет базовую обертку, но не хватает строгой типизации для сложных ответов.
2.  **Обновление `TECHNICAL_SPEC.md`**:
    *   Добавлен раздел **"Текущий статус"**, чтобы разработчики понимали точку отсчета.
    *   Эпики разбиты на более детальные задачи (например, `EXP-01 Оптимизация рендеринга`, `EDT-01 Синхронизация с Бэкендом`).
    *   Добавлены конкретные технические решения: использование `useRef` для vis-network, внедрение `Toast` уведомлений.
    *   Описаны контракты данных (интерфейсы `GraphNode`, `GraphEdge`) и формат ошибок.
    *   Уточнены критерии приемки (UX, типизация).

Теперь ТЗ отражает реальные потребности проекта: переход от прототипа к production-ready приложению с фокусом на интеграцию API и улучшение UX. Документ готов к использованию командой разработки.