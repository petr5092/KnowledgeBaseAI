# KnowledgeBaseAI

React + TypeScript + Vite приложение для визуализации и управления графом знаний, интегрированное с ИИ-ассистентом.

---

## 🛠️ Запуск и Разработка

Для корректной работы приложения (включая граф, поиск и ИИ) требуется запущенная инфраструктура (Neo4j, Postgres, FastAPI).

### 🐳 Способ 1: Полный запуск через Docker (Рекомендуется)

Это самый быстрый способ поднять все сервисы одной командой.

1.  **Настройка окружения:**
    ```bash
    cp .env.example .env.prod
    # Проверьте настройки в .env.prod (пароли, хосты)
    ```

2.  **Запуск всех контейнеров:**
    ```bash
    docker-compose up -d --build
    ```

3.  **Инициализация данных:**
    Если база данных пуста, запустите скрипты загрузки данных внутри контейнера:
    ```bash
    # Загрузка графа в Neo4j
    docker exec knowledgebase-fastapi-dev-1 python scripts/load_data.py
    docker exec knowledgebase-fastapi-dev-1 python scripts/push_to_neo4j.py
    ```

4.  **Доступ:**
    *   Frontend: `http://localhost:5173`
    *   Backend API: `http://localhost:8000/docs`

---

### 💻 Способ 2: Локальная разработка фронтенда

Если бэкенд уже запущен (в Docker или удаленно), вы можете запустить фронтенд локально для быстрой HMR-разработки.

1.  **Установка зависимостей:**
    ```bash
    cd frontend
    npm install
    ```

2.  **Настройка API:**
    Убедитесь, что в коде или переменных окружения указан правильный URL бэкенда (обычно `http://localhost:8000`).

3.  **Запуск:**
    ```bash
    npm run dev
    ```

---

## [EXP-03] Фильтрация по типам (Kind)

Для управления сложностью графа реализован механизм динамической фильтрации по типам.

1.  **Авто-обнаружение типов:**
    *   При загрузке данных (`viewport`) анализируются все типы узлов (`kind`).
    *   Используется `useMemo` для формирования списка уникальных категорий.
2.  **Интерфейс фильтра:**
    *   В верхней панели добавлен селектор `KBSelect` (Kind).
    *   Пользователь может выбрать конкретный тип (например, "Concept") или "All".
3.  **Реактивная перерисовка:**
    *   При смене фильтра массив узлов мгновенно обновляется без перезагрузки страницы.

---

## ⚠️ Технические детали

*   **Сохранение состояния:** Внедрен `GraphContext`, который сохраняет положение камеры, позиции узлов и выбранную глубину при переходах между страницами.
*   **useRef:** Используется для прямого доступа к инстансу `vis-network` и актуальным координатам без триггера лишних ре-рендеров.
*   **Анимации:** Сайдбар и чат используют CSS-переходы (`transition`) и динамические классы `.open` для плавного появления.

---

## 🚀 Changelog (Эпик 2: Модуль "Explore")

### [EXP-03] Фильтрация по типам (Kind)
*   **Задача:** Предоставить инструмент для очистки графа от "шума".
*   **Решение:** Реализовано динамическое извлечение типов из `viewport` и реактивная фильтрация через `KBSelect`.
