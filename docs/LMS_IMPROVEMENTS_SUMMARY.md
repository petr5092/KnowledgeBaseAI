# Техническое задание: Улучшение LMS интеграции KnowledgeBaseAI + StudyNinja

**Версия:** 1.0
**Дата:** 2026-02-03
**Статус:** Ready for Implementation

---

## Обзор проекта

### Архитектура

**KnowledgeBaseAI** (`/root/KnowledgeBaseAI`, ветка `feat-lms-integration`):
- Stateless-сервис работы с графом знаний (Neo4j)
- Предоставляет REST API для LMS систем
- Генерирует: вопросы, тесты, дорожные карты, аналитику

**StudyNinja** (`/root/StudyNinja-API`, ветка `latest`):
- LMS система, клиент KnowledgeBaseAI
- Генерирует микро-уроки (I Do / We Do / You Do)
- Управляет прогрессом обучения

---

## Задачи и решения

### 📊 Задача 1: Синхронизация генерации вопросов и визуализации

**Проблема:**
- Вопрос и визуализация генерируются одновременно → несоответствия
- Текст ссылается на несуществующие метки визуализации
- Retry-логика (2 попытки) не решает проблему

**Решение:** Двухэтапная генерация
1. **Этап 1**: Создать структуру визуализации с метками (ПЕРЕД вопросом)
2. **Этап 2**: Генерировать вопрос с привязкой к существующим меткам
3. **Валидация**: Проверить синхронизацию (метки в тексте = метки в визуализации)
4. **Retry**: Одна попытка исправления с явным указанием ошибок

**Изменения:**
- **KB**: Новый сервис `question_visual_sync.py`
- **KB**: Модификация `engine.py` (строки 851-1222)

**Метрики:**
- Sync validation error rate: < 2% (текущий ~15-20%)
- Generation latency: < 4 сек

**Детали:** См. `LMS_INTEGRATION_TECHNICAL_SPEC.md`, раздел "Задача 1"

---

### 🎯 Задача 2: Адаптивное тестирование с объективной оценкой

**Проблема:**
- Простая адаптация сложности (+1/-1)
- Нет статистической оценки надежности результата
- Финальный скор = простое среднее

**Решение:** Item Response Theory (IRT)
1. **2PL модель**: P(correct | theta, difficulty, discrimination)
2. **Maximum Information**: Выбор следующего вопроса для максимальной точности
3. **MLE theta**: Оценка способностей методом Ньютона-Рафсона
4. **Критерии завершения**: SE <= 0.35 ИЛИ достаточная информация (I >= 12)

**Изменения:**
- **KB**: Новый сервис `irt_assessment.py`
- **KB**: Модификация assessment endpoints (`engine.py`)
- **KB**: Добавить `scipy` в requirements

**Метрики:**
- Средняя длина теста: < 12 вопросов (текущий ~15-20)
- Reliability: > 0.80
- Theta SE: < 0.35

**Детали:** См. `LMS_INTEGRATION_TECHNICAL_SPEC.md`, раздел "Задача 2"

---

### 🗺️ Задача 3: Повышение релевантности дорожной карты

**Проблема:**
- Roadmap генерируется только на основе PREREQ связей
- Недостаточный датасет связей в графе
- Не учитывается семантическая близость тем

**Решение:** Гибридная рекомендательная система
1. **Graph component** (30%): Структура графа (importance, unlock_count)
2. **Semantic component** (35%): Embeddings similarity (OpenAI API)
3. **Skill matching** (25%): Соответствие навыкам пользователя
4. **Difficulty ZPD** (10%): Оптимальная сложность (theta ± 0.5)

**Формула скора:**
```
score = 0.30 * graph_score +
        0.35 * semantic_score +
        0.25 * skill_score +
        0.10 * difficulty_score
```

**Изменения:**
- **KB**: Новый сервис `hybrid_roadmap.py`
- **KB**: Модификация roadmap endpoint (`engine.py`)
- **KB**: Redis кэширование embeddings (TTL 7 дней)
- **SN**: Минимальные изменения (новые поля в ответе)

**Метрики:**
- User satisfaction rating: > 4.0/5.0
- Completion rate узлов: > 70%

**Детали:** См. `REMAINING_TASKS_3_TO_6.md`, раздел "Задача 3"

---

### 🎨 Задача 4: Генерация координат для микро-уроков

**Проблема:**
- Микро-уроки генерируются БЕЗ визуализации
- Фазы We Do / You Do нуждаются в визуальной поддержке
- Нет интеграции с KB visualization engine

**Решение:** Расширить генерацию микро-уроков
1. Определить необходимость визуализации (ключевые слова)
2. Запросить визуализацию из KB API
3. Добавить `visualization` поле в payload фаз We Do / You Do

**Структура payload:**
```json
{
    "we_do": {
        "problem": {"text": "...", "correctAnswer": "..."},
        "visualization": {
            "shapes": [...],
            "labels": ["A", "B", "C"],
            "coordinate_system": "cartesian"
        }
    }
}
```

**Изменения:**
- **SN**: Модификация `adaptive_engine.py`
- **KB**: Новый endpoint `/v1/graph/generate_visualization`

**Метрики:**
- Visual generation success rate: > 95%
- Integration latency: < 2 сек

**Детали:** См. `REMAINING_TASKS_3_TO_6.md`, раздел "Задача 4"

---

### ✅ Задача 5: Тестирование системы прогресса

**Проблема:**
- Система прогресса не покрыта тестами
- Возможны race conditions при обновлении
- Нет гарантии consistency

**Решение:** Comprehensive testing + атомарные обновления
1. **Unit-тесты**: `test_progress_system.py`
   - Завершение микро-урока
   - Разблокировка следующего узла
   - Remediation lessons
   - Roadmap total_score
2. **Атомарные обновления**: SQL запросы с транзакциями
3. **Логирование событий**: Audit trail для всех изменений прогресса

**Изменения:**
- **SN**: Создать `tests/test_progress_system.py`
- **SN**: Рефакторинг `lesson_service.py` (атомарные обновления)
- **SN**: Добавить логирование событий прогресса

**Метрики:**
- Test coverage: 100% для progress logic
- Progress calculation accuracy: 100%

**Детали:** См. `REMAINING_TASKS_3_TO_6.md`, раздел "Задача 5"

---

### 🔄 Задача 6: Адаптивная дорожная карта (регенерация)

**Проблема:**
- Roadmap статична после генерации
- Не адаптируется к прогрессу пользователя
- Следующие темы могут быть неоптимальными

**Решение:** Динамическая регенерация после завершения узла
1. **Trigger**: Завершение узла → запуск фоновой задачи (Celery)
2. **Обновление прогресса**: Получить новый mastery, theta пользователя
3. **Запрос KB**: Новые рекомендации с focus на завершенную тему
4. **Обновление узлов**: Заменить/дополнить следующие 2-3 узла

**Алгоритм:**
```
1. User завершает node N
2. Celery task: regenerate_next_nodes(node_N)
3. Получить updated user_progress, user_theta
4. KB API: roadmap с focus_topic_uid = node_N.topic_uid
5. Обновить nodes N+1, N+2, N+3:
   - N+1: status = 'available'
   - N+2, N+3: status = 'locked'
6. Сохранить изменения
```

**Изменения:**
- **SN**: Новая фоновая задача `tasks/adaptive_roadmap.py`
- **SN**: Конфигурация Celery
- **SN**: Интеграция в `lesson_service.py`

**Метрики:**
- Regeneration latency: < 5 сек
- User retention after regeneration: > 85%

**Детали:** См. `REMAINING_TASKS_3_TO_6.md`, раздел "Задача 6"

---

## Приоритезация и план реализации

### Фаза 1: Качество контента (Недели 1-2)
**Приоритет: ВЫСОКИЙ**
- ✅ Задача 1: Синхронизация вопросов и визуализации
- ✅ Задача 4: Визуализация микро-уроков

**Обоснование:** Прямо влияет на UX, видно пользователям

### Фаза 2: Измерение знаний (Недели 3-4)
**Приоритет: ВЫСОКИЙ**
- ✅ Задача 2: IRT адаптивное тестирование
- ✅ Задача 5: Тестирование системы прогресса

**Обоснование:** Критично для объективной оценки, foundation для задачи 6

### Фаза 3: Персонализация (Недели 5-6)
**Приоритет: СРЕДНИЙ**
- ✅ Задача 3: Гибридные рекомендации roadmap
- ✅ Задача 6: Адаптивная roadmap

**Обоснование:** Улучшает retention, требует данные из фазы 2

---

## Технические зависимости

### KnowledgeBaseAI
```python
# requirements.txt (добавить)
scipy>=1.11.0
numpy>=1.24.0
openai>=1.0.0  # для embeddings
```

### StudyNinja
```python
# requirements.txt (добавить)
celery>=5.3.0
redis>=5.0.0
```

**Конфигурация:**
```bash
# .env
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

KB_API_URL=https://api.kb.studyninja.ru
KB_API_KEY=<your_key>

OPENAI_API_KEY=<your_key>  # для embeddings
```

---

## Мониторинг и метрики

### Dashboard метрик (создать)

**Качество контента:**
- `question_visual_sync_error_rate` (target: < 2%)
- `visual_generation_success_rate` (target: > 95%)

**Адаптивное тестирование:**
- `average_test_length` (target: < 12 вопросов)
- `average_theta_se` (target: < 0.35)
- `average_reliability` (target: > 0.80)

**Персонализация:**
- `roadmap_node_completion_rate` (target: > 70%)
- `roadmap_regeneration_latency_ms` (target: < 5000)
- `user_satisfaction_rating` (target: > 4.0/5.0)

**Система прогресса:**
- `progress_calculation_errors` (target: 0)
- `progress_update_latency_ms` (target: < 100)

---

## Риски и митигации

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Embeddings API latency | Средняя | Redis кэширование (TTL 7 дней) |
| IRT калибровка требует данные | Высокая | Начать с оценочных параметров, калибровать постепенно |
| Celery очередь перегружена | Низкая | Rate limiting, приоритизация задач |
| Визуализация не генерируется | Средняя | Fallback на текстовое описание |
| Roadmap регенерация слишком частая | Низкая | Debounce: макс 1 раз в 24 часа на roadmap |

---

## Чеклист перед деплоем

### KnowledgeBaseAI
- [ ] Все unit-тесты проходят
- [ ] Feature flags настроены для A/B тестирования
- [ ] Redis кэш настроен и работает
- [ ] OpenAI API key сконфигурирован
- [ ] Мониторинг метрик подключен
- [ ] Документация API обновлена

### StudyNinja
- [ ] Все integration-тесты проходят
- [ ] Celery worker запущен и стабилен
- [ ] KB API client обновлен с новыми endpoints
- [ ] Database migrations применены
- [ ] Логирование событий прогресса работает
- [ ] UI обновлен для новых полей (reasons, visualization)

---

## Контакты и ресурсы

**Документация:**
- Детальные алгоритмы: `LMS_INTEGRATION_TECHNICAL_SPEC.md`
- Практические инструкции: `REMAINING_TASKS_3_TO_6.md`

**Ключевые файлы для модификации:**

**KnowledgeBaseAI:**
- `backend/app/services/question_visual_sync.py` (новый)
- `backend/app/services/irt_assessment.py` (новый)
- `backend/app/services/hybrid_roadmap.py` (новый)
- `backend/app/api/engine.py` (модификация)

**StudyNinja:**
- `backend/app/domain/services/ai_services/adaptive_engine.py` (модификация)
- `backend/app/domain/services/api_services/lesson_service.py` (модификация)
- `backend/app/tasks/adaptive_roadmap.py` (новый)
- `backend/tests/test_progress_system.py` (новый)

---

## Итоговые метрики успеха

### KPIs проекта

| Метрика | Текущее значение | Целевое значение | Измерение |
|---------|------------------|------------------|-----------|
| Question-visual sync errors | ~15-20% | < 2% | % некорректных вопросов |
| Средняя длина теста | ~15-20 вопросов | < 12 вопросов | среднее |
| Test reliability | Не измеряется | > 0.80 | IRT reliability |
| Roadmap completion rate | ~40-50% | > 70% | % завершенных узлов |
| User satisfaction | ~3.5/5.0 | > 4.0/5.0 | survey rating |
| Visual generation success | Не применяется | > 95% | % успешных генераций |

### Timeline
- **Фаза 1** (Недели 1-2): Качество контента → +30% UX improvement
- **Фаза 2** (Недели 3-4): Измерение знаний → +40% assessment accuracy
- **Фаза 3** (Недели 5-6): Персонализация → +50% retention rate

**Общий срок:** 6 недель
**Ожидаемый ROI:** +60% user engagement, +40% knowledge retention

---

**Готово к реализации!** 🚀
