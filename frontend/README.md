# KnowledgeBaseAI Frontend

## 📖 Обзор проекта
Это фронтенд-часть приложения **KnowledgeBaseAI**, представляющая собой интерфейс для визуализации, редактирования и анализа графа знаний. Приложение построено как Single Page Application (SPA) с использованием современных веб-технологий.

Ключевые возможности:
- **Визуализация графа**: Интерактивный просмотр узлов и связей (используется `vis-network` и `d3`).
- **Редактор**: Инструменты для создания и редактирования структуры графа (используется `reactflow`).
- **AI Ассистент**: Встроенный чат-бот для контекстных вопросов по графу, генерации учебных планов и аналитики.
- **Аналитика**: Просмотр статистики по использованию и качеству графа.

## 🛠 Технологический стек

### Основное
- **Core**: [React 19](https://react.dev/)
- **Build Tool**: [Vite 7](https://vitejs.dev/)
- **Language**: [TypeScript](https://www.typescriptlang.org/)
- **Routing**: [React Router v7](https://reactrouter.com/)

### Визуализация
- **vis-network**: Для режима исследования ("Explore").
- **React Flow**: Для режима редактирования ("Edit").
- **D3.js**: Для вспомогательных визуализаций и виджетов.

### API и Данные
- **Custom Fetch Wrapper**: Типизированная обертка над `fetch` с обработкой ошибок (`src/api.ts`).
- **State Management**: Локальный стейт React + Custom Observer pattern для логов транзакций (`TxLog`).

## 🚀 Установка и запуск

### Предварительные требования
- Node.js (рекомендуется v18 или выше)
- npm или yarn

### Установка зависимостей
```bash
cd frontend
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:5173`.
Vite настроен на проксирование API запросов:
- `/v1/*` -> `https://api.kb.xteam.pro`
- `/ws/*` -> `wss://api.kb.xteam.pro`

### Сборка для продакшена
```bash
npm run build
```
Результат сборки будет находиться в директории `dist`.

### Линтинг
```bash
npm run lint
```

## 📂 Структура проекта

```
frontend/src/
├── assets/           # Статические ресурсы (изображения, иконки)
├── pages/            # Компоненты страниц (маршруты)
│   ├── ExplorePage.tsx   # Главная страница просмотра графа
│   ├── EditPage.tsx      # Страница редактирования
│   ├── AnalyticsPage.tsx # Аналитика
│   ├── RoadmapPage.tsx   # Дорожная карта обучения
│   ├── PracticePage.tsx  # Режим практики
│   ├── SettingsPage.tsx  # Настройки
│   ├── ImpactGraph.tsx   # Визуализация влияния изменений
│   └── ReviewDiff.tsx    # Ревью изменений
├── store/            # Управление состоянием
│   └── txLog.ts      # Лог транзакций (Observer pattern)
├── utils/            # Утилиты
│   └── inversePatch.ts
├── widgets/          # Переиспользуемые виджеты
│   └── d3.ts
├── api.ts            # Клиент API и типы данных
├── App.tsx           # Корневой компонент, лейаут и роутинг
├── main.tsx          # Точка входа
└── vite.config.ts    # Конфигурация Vite и Proxy
```

## 🌐 Конфигурация и окружение

### Переменные окружения (.env)
Приложение поддерживает внедрение конфигурации через переменные окружения или глобальный объект `window.__ENV__` (для Docker/Runtime конфигурации).

| Переменная | Описание |
|------------|----------|
| `VITE_API_BASE_URL` | Базовый URL для API (если не используется прокси) |

### Настройка Proxy (vite.config.ts)
В режиме разработки запросы к API проксируются для обхода CORS и упрощения разработки:
```typescript
proxy: {
  '/v1': { target: 'https://api.kb.xteam.pro', changeOrigin: true },
  '/ws': { target: 'wss://api.kb.xteam.pro', ws: true }
}
```

## 🔌 API Интеграция
Взаимодействие с бэкендом организовано через модуль `src/api.ts`.
Он предоставляет типизированные функции для основных операций:
- `assistantChat`: Отправка сообщений AI ассистенту.
- `getViewport`: Получение подграфа для визуализации.
- `postRoadmap`: Генерация дорожной карты.
- `getAnalyticsStats`: Получение метрик.

Пример использования:
```typescript
import { assistantChat } from './api';

const response = await assistantChat({
  message: "Как связаны эти узлы?",
  action: "explain_relation",
  // ...параметры контекста
});
```

## 🤖 AI Ассистент
Ассистент интегрирован глобально в `App.tsx` и доступен на любой странице. Он поддерживает контекстные действия (`action`), которые позволяют фронтенду запрашивать специфические форматы ответов (например, JSON для построения графа или текст для объяснения).
